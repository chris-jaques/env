#!/bin/sh
# 
# Source the Env
#

# Pull latest version of env
git -C ~/env pull origin master &> /dev/null;

# source all '.al', used as plugin system for env
for al in $(find ~/env -name '*.al'); do . $al; done
